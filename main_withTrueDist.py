from process_withTrueDist import main
from process_base_withTrueDist import main_base


if __name__ == "__main__":
    random_seeds = range(0, 10)
    main(random_seeds)
    main_base(random_seeds)

    random_seeds = range(10, 20)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(20, 30)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(30, 40)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(40, 50)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(50, 60)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(60, 70)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(70, 80)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(80, 90)
    main(random_seeds)
    main_base(random_seeds)
    
    random_seeds = range(90, 100)
    main(random_seeds)
    main_base(random_seeds)
